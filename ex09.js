// Create a class called 'Vehicle' with properties 'manufacturer' and 'model'.
// Implement getters and setters for these properties.
// Create a subclass 'Car' that extends 'Vehicle', adding properties 'numberOfDoors' and 'carStatus' with getters and setters.
// Create a subclass 'ElectricCar' from 'Car', adding properties 'batteryCapacity' and 'range'.
// Implement getters and setters for the new properties in 'ElectricCar'.
// Inside the range setter, create an if else statement that if range is below 100, set carStatus to 'New'. Else, set carStatus to 'Used'. Use the carStatus setter.
// Create a method displayCarInfo that returns manufacturer, model, numberOfDoors, carStatus, batteryCapacity, range in a sentence.
// Create an instance of ElectricCar and console log the displayCarInfo method.

class Vehicle {

}